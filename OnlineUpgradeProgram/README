在线升级程序，通过web界面包升级包上传到设备，下次重新启动后，执行升级包里的脚本，对程序升级

1, arm-none-linux-gnueabi目录下是一些操作内核、文件系统、uboot地址空间的工具，可以拷贝、擦除、写入这些地址空间，实现在系统启动状态下升级内核、文件系统、uboot。由mtd-utils开源工具提供。
2， kernel-nand_可擦写目录下，是一个经过修改了的内核，该内核释放了对内核、文件系统、uboot地址空间修改的限制。
3， mtd-utils目录是一些开源工具。
4， uboot-硬校验目录是把uboot默认的软校验改为了硬校验。因为内核经过上面的修改后，uboot会在校验时报错。
5, upgrade_package目录下的online_upgrade.sh脚本会执行command目录下的可执行程序，用提供的内核等替换系统当前内核。